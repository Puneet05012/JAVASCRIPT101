<!DOCTYPE html>
<html>
<head>
  <title>JavaScript Slides</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>JavaScript Slides</h1>
  <ul>
    <% slides.forEach((slide, index) => { %>
      <li><a href="/slides/<%= slide.replace('.js', '').replace('.html', '') %>">Slide <%= index + 1 %>: <%= slide %></a></li>
    <% }) %>
  </ul>
</body>
</html>
